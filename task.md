# Tasks - Dark Mode Galaxy Polish

- [ ] **CSS Implementation (`globals.css`)**
  - [ ] **Galaxy Gradient**: Create a rich, deep purple/indigo nebula effect using `radial-gradient` in `--bg-image-base`.
  - [ ] **Star Animation**: Define `@keyframes twinkle` (opacity/scale).
  - [ ] **Star Layers**: Create classes `.stars-sm`, `.stars-md` using CSS `box-shadow` for randomized star positions (performant hack).
- [ ] **Component Update (`page.tsx`)**
  - [ ] **Structure**: Add a static `<div id="star-field">` container.
  - [ ] **Visibility**: Ensure it only shows in `.dark` via CSS rules (not JS).
- [ ] **Verification**
  - [ ] Check galaxy depth.
  - [ ] Check star twinkling.
  - [ ] Confirm text contrast.
